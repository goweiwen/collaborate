import React from 'react';
import PropTypes from 'prop-types';
import { Menu, Item, Header } from 'semantic-ui-react';

const Sidebar = (props) =>
  <Menu inverted vertical fixed='left' style={{ width: '250px' }}>
  </Menu>

export default Sidebar;
